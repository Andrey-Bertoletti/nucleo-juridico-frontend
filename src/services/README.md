# src/services

Cliente HTTP central para a API FastAPI, integração com Supabase (Auth e Storage) e interceptors (token, erros). Cada feature pode expor seu próprio serviço derivado deste cliente base.

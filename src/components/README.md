# src/components

Componentes de UI **genéricos e reutilizáveis** (Button, Input, Card, Modal, Table, etc.). Sem regra de negócio. Componentes específicos de um domínio devem ficar em `features/<dominio>/components`.

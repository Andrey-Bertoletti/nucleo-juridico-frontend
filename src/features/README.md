# src/features

Módulos por **domínio do sistema** (ex.: `atendimento`, `clientes`, `triagem`, `casos`, `documentos`, `usuarios`). Cada feature pode ter sua própria pasta `components/`, `hooks/` e `services/` internos. Idealmente sem dependência cruzada entre features.
